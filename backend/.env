# JWT secret — change this to a long random string in production
JWT_SECRET=hamro_cloud_secret_key_change_in_prod

# The URL your frontend runs on (used for CORS)
# If using VS Code Live Server, this is typically:
FRONTEND_URL=http://127.0.0.1:5500

# The backend's own base URL (used when generating share/reset links)
BASE_URL=http://127.0.0.1:3000

# Port the backend listens on
PORT=3000
email="kalyan.kalyan777@gmail.com"
password=""