{
  "name": "backend",
  "version": "1.0.0",
  "description": "Node.js + Express REST API for the Hamro Cloud frontend.   Uses a local `db.json` file as the database (no external DB setup needed).   Swap the `db.js` store for a real DB (PostgreSQL / MongoDB) when scaling up.",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "uuid": "^14.0.0"
  }
}
